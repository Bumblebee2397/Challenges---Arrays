#include<iostream>
#include<cstring>
using namespace std;

int main(){

	int n;
	cin>>n;

	int arr[n],max=0,i;

	if(n>0){

	for(i=0;i<n;i++){

		cin>>arr[i];
		if(arr[i]>max)
			max=arr[i];
	}
}

	cout<<max;
	return 0;
}
